apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-crosspod
spec:
  ingress:
  - from:
  namespaceSelector:
    matchLabels:
      app.kubernetes.io/instance: immich-app
  policyTypes:
  - Ingress

